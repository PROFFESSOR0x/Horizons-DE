# hyprlang noerror false
# Legacy (.conf) user overrides for Hyprland < 0.55 — mirror of custom/variables.lua.
# Override any $var from hyprland/variables.conf here, e.g.:
#   $terminal = kitty
# This file is never overwritten by updates (installed once, then left alone).
# For all available variables, see hyprland/variables.conf
