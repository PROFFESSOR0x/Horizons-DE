# hyprlang noerror false
# Legacy (.conf) user overrides for Hyprland < 0.55 — mirror of custom/rules.lua.
# You can put custom rules here.
# This file is never overwritten by updates (installed once, then left alone).
# Window/layer rules: https://wiki.hyprland.org/Configuring/Window-Rules/
# Workspace rules: https://wiki.hyprland.org/Configuring/Workspace-Rules/

# ######## Window rules ########

# Uncomment to apply global transparency to all windows:
# windowrulev2 = opacity 0.89 override 0.89 override, match:class .*

# Disable blur for all xwayland apps
# windowrulev2 = noblur on, match:xwayland 1
