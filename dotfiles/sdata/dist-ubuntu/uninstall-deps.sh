# This script is meant to be sourced.
# It's intentionally conservative: Ubuntu packages installed here are common
# desktop/runtime packages and may have existed before Horizons.

printf "${STY_YELLOW}[$0]: Ubuntu dependency uninstall is conservative; leaving apt packages installed.${STY_RST}\n"
printf "${STY_YELLOW}[$0]: Remove packages manually if this machine was dedicated to Horizons only.${STY_RST}\n"
